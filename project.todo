# Project TODO

## Backlog
- [ ] Add 3D preview display using popup window for selected model (3d view button)
- [ ] Build metadata edit dialog and persistence
- [ ] Make 3d model preview work with 3mf files
- [ ] update program to reflect v0.030
- [ ] v0.30 release notes
- [ ] remove windows warning
v0.30 misc updates (for github copilot):
make the app show a loading bar instead of not responding when loading large file, also save mode (microsd/local) in config json so it is used as the default in save location dropdown in the add model menu and also save the theme every time it is switched. make sure "system volume information doesnt show as a model lmao

## Completed
- [x] Load models from configuration and hydrate gallery cards
- [x] Enable search, sorting, and material filtering controls
- [x] Implement import workflow for new models (dialog + packaging)
- [x] Generate STL thumbnails with adaptive cropping and caching
- [x] Add loading overlay sourced from UI file for async refreshes
- [x] Apply theme toggle with tinted icons and persisted preference
- [x] Modernize scrollbars in light and dark themes
- [x] Implement "Add Model" button to show popup
- [x] Make "Import" button select or create a models folder and persist configuration
- [x] Package Windows release (PyInstaller bundle + Inno Setup installer)

## Release Notes
### 0.20 Beta (2025-11-03)
- Config saved under `%APPDATA%\zPrint` with document-root fallback.
- Import flow supports creating new model folders and updates gallery empty state.
- Added Windows build pipeline: PyInstaller spec, release script, and Inno Setup installer.
- Embedded official logo into executable, installer, and shortcuts.

### 0.10 Beta (initial drop)
- Initial gallery experience with search, sort, and material filters.
- Theme toggle, loading overlay, and STL thumbnail rendering pipeline.
- Added New Model dialog scaffold and JSON-backed gallery hydration.













